import React from "react";
import Layout from "../layout/Layout";
import FileManagerContent from "./FileManagerContent";
const Email = () => {
  return <Layout component={<FileManagerContent />} />;
};

export default Email;
