import React from "react";
import Layout from "../layout/Layout";
import PermissionContent from "./PermissionContent";

const Permission = () => {
  return <Layout component={<PermissionContent />} />;
};

export default Permission;
