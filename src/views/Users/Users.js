import React from "react";
import Layout from "../layout/Layout";
import UserContent from "./UserContent";

const Contacts = () => {
  return <Layout component={<UserContent />} />;
};

export default Contacts;
