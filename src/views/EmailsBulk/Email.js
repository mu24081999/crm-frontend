import React from "react";
import Layout from "../layout/Layout";
import EmailContent from "./EmailContent";

const Email = () => {
  return <Layout component={<EmailContent />} />;
};

export default Email;
