import React from "react";
import Layout from "../layout/Layout";
import TermsContent from "./TermsContent";

const Terms = () => {
  return <Layout component={<TermsContent />} />;
};

export default Terms;
