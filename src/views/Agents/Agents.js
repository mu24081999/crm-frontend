import React from "react";
import Layout from "../layout/Layout";
import AgentContent from "./AgentContent";

const Contacts = () => {
  return <Layout component={<AgentContent />} />;
};

export default Contacts;
