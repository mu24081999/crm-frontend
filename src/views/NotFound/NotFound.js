import React from "react";
import Layout from "../layout/Layout";
import NotFoundContent from "./NotFoundContent";

const NotFound = () => {
  return <Layout component={<NotFoundContent />} />;
};

export default NotFound;
