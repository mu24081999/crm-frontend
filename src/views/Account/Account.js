import React from "react";
import Layout from "../layout/Layout";
import AccountContent from "./AccountContent";

const Account = () => {
  return <Layout component={<AccountContent />} />;
};

export default Account;
